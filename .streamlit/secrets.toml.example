# Copy this file to secrets.toml and fill in your keys.
# secrets.toml is gitignored — never commit real keys.
# On Streamlit Cloud, set these in the app dashboard instead.

GEMINI_API_KEY = ""
TAVILY_API_KEY = ""
